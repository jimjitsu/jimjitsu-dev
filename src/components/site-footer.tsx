/**
 * @deprecated Replaced by the social row at the bottom of the sidebar
 * (see `src/components/sidebar.tsx`). This file is kept only because the
 * sandbox couldn't delete it; safe to remove once you can rm it locally.
 */
export {};
