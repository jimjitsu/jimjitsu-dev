export const CONTACT_EMAIL = "jimbo.c.tierney@gmail.com";
// www is the canonical/primary host — the apex (jimjitsu.dev) 307-redirects to it.
// Every canonical tag, the sitemap, OG/Twitter URLs, RSS, and JSON-LD derive from this.
export const SITE_URL = "https://www.jimjitsu.dev";
